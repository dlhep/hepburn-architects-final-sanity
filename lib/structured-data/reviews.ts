// Self-serving aggregate ratings are deliberately omitted. Individual Review
// entities should only be added if policy and visible attribution are re-audited.
export function buildReviewCollectionSchema() { return undefined; }
